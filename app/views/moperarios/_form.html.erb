<%= simple_form_for(@moperario) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :imodulo %>
    <%= f.association :operario %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>

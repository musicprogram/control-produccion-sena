<%= simple_form_for(@moperacion) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :omaquina %>
    <%= f.association :operacion %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>

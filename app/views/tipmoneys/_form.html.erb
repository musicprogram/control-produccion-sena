<%= form_for(@tipmoney) do |f| %>
  <% if @tipmoney.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tipmoney.errors.count, "error") %> prohibited this tipmoney from being saved:</h2>

      <ul>
      <% @tipmoney.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :siglas %><br>
    <%= f.text_field :siglas %>
  </div>
  <div class="field">
    <%= f.label :descripcion %><br>
    <%= f.text_area :descripcion %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

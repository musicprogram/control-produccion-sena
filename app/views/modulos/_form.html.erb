<%= form_for(@modulo) do |f| %>
  <% if @modulo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@modulo.errors.count, "error") %> prohibited this modulo from being saved:</h2>

      <ul>
      <% @modulo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

 <div class="field">
   <%= f.label :Tipo_Vestuario %>
   <%= f.collection_select :tipovestuario_id, Tipovestuario.all, :id, :nombre, prompt: "Seleccione un tipo de vestuario" %>
 </div>
  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
    <%= f.label :descripcion %><br>
    <%= f.text_field :descripcion %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

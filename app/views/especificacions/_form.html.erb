<%= form_for(@especificacion) do |f| %>
  <% if @especificacion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@especificacion.errors.count, "error") %> prohibited this especificacion from being saved:</h2>

      <ul>
      <% @especificacion.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :descripcion %><br>
    <%= f.text_area :descripcion %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

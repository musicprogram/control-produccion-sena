<%= form_for(@tioperacion) do |f| %>
  <% if @tioperacion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tioperacion.errors.count, "error") %> prohibited this tioperacion from being saved:</h2>

      <ul>
      <% @tioperacion.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

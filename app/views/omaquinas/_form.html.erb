<%= simple_form_for(@omaquina) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :moperario %>
    <%= f.association :maquina %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>

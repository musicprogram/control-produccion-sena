<%= form_for(@cateayuda) do |f| %>
  <% if @cateayuda.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cateayuda.errors.count, "error") %> prohibited this cateayuda from being saved:</h2>

      <ul>
      <% @cateayuda.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

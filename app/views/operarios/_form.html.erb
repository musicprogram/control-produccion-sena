<%= form_for(@operario) do |f| %>
  <% if @operario.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@operario.errors.count, "error") %> prohibited this operario from being saved:</h2>

      <ul>
      <% @operario.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
   <div class="field">
     Tipo de Documento <br>
    <%= f.collection_select :documento_id, Documento.all, :id, :nombre, class: "form-control", prompt: "Seleccione tipo de Documento" %>
  </div>
  <div class="field">
    <%= f.label :documento %><br>
    <%= f.text_field :numdocumento %>
  </div>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  
  <div class="field">
    <%= f.label :apellido %><br>
    <%= f.text_field :apellido %>
  </div>
  <div class="field">
    <%= f.label :email %><br>
    <%= f.text_field :email %>
  </div>
  <div class="field">
     Grupo <br>
    <%= f.collection_select :grupo_id, Grupo.all, :id, :nombre, class: "form-control", prompt: "Seleccione Grupo" %>
  </div>
  
  <div class="field">
    <%= f.label :ayuda_id %><br>
    <%= f.text_field :ayuda_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

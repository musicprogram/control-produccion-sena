<div class="nested-field">
    <%= f.collection_select :operario_id, Operario.all, :id, :nombre, prompt: 'Seleccione Operario' %>
    <%= link_to_remove_association "Eliminar Operario", f %>
    
     <div class="fielset well">
        <%= f.simple_fields_for :omaquinas do |omaquina| %>
            <%= render 'omaquina_fields', f: omaquina %>
        <% end %>   
        <%= link_to_add_association 'Agregar Maquina', f, :omaquinas %>
  </div>
</div>
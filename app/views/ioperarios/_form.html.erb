<%= simple_form_for(@ioperario) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :tinstructor %>
    <%= f.association :operario %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>

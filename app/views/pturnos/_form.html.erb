<%= simple_form_for(@pturno) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :proyecto %>
    <%= f.association :turno %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
